spring:
  jpa:
    hibernate:
      ddl-auto: create-drop 
    show-sql: false
  
  liquibase:
    enabled: false
  
  datasource:
    driver-class-name: org.postgresql.Driver

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false